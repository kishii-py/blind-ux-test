---
name: blind-ux-test
description: 実装した画面が「初めて使う人」に通じるかを、何も知らされていない AI ペルソナに実際に操作させて測る。完了バイアス対策として、達成できたかではなく「どこで詰まったか」を証拠にする。「初見テスト」「UX 監査」「使いやすいか確認して」「分かりにくくないか見て」で使用。
---

# blind-ux-test — 初見ユーザーによる直感性監査

## あなたの役割

あなたは**オーケストレータ**です。実際に画面を触るのは、何も知らされていない別の AI
（`ux-blind-tester` ペルソナ）。あなたは準備・起動・回収・レポートを担当します。

```
  ユーザー
    │  「この画面、初見で使える？」
    ▼
  あなた（オーケストレータ）
    │  ① 対象を調べる  ② ゴールを作る  ③ 環境を整える
    │  ④ ペルソナを起動  ⑤ 回収して答え合わせ  ⑥ レポート
    ▼
  ペルソナ（何も知らされていない AI）
       画面を撮る・クリックする・文字を打つ
```

🚨 **あなた自身が画面を触って「分かりやすいですね」と判断してはいけません。**
あなたは対象システムを調べた後なので、初見の判断ができません（知識の呪い）。
判断材料はペルソナの行動だけです。

🚨 **ペルソナが「達成した」ことを成果にしないでください。** AI は賢すぎて、
人間が「複雑すぎる」と評価したシステムでも完走します。**証拠になるのは詰まった事実だけ**です。

| ペルソナの結果 | 扱い |
|---|---|
| 賢い AI でも詰まった | **強い証拠**。人間なら確実に詰まる |
| 賢い AI が達成した | ほぼ無価値。手数（経路情報）だけ採用 |

---

## 手順

### 1. 対象を調べる（あなたが知る。ペルソナには絶対に教えない）

ゴールが実行不可能だったり、正解が採れなかったりすると走行が丸ごと無駄になります。
先に自分で把握してください。

- **画面地図** — URL / 画面 / メニュー構造
- **権限ゲート** — 走行に使うユーザーが目的の画面に入れるか（**入れないと走行が詰む**）
- **データ在庫** — ゴール素材になる実データ（取引先名・品番など）、空テーブルの有無
- **ガードの有無** — 削除ガード・重複チェック（エラー系ゴールが成立する条件）

### 2. 業務ゴールを作る

ゴールの型は **`references/scenario-patterns.md` の 12 パターン**から選びます。
1 回の監査で 2〜3 パターン組み合わせると効率的です。

**書き方の絶対条件:**

- **業務の言葉だけで書く**。「この受注を出荷できる状態にして」
- 🚨 **UI 用語を書いた時点で失格**。ボタン名・画面名・タブ名・モーダルという語が
  ゴール文に入っていないか、渡す前に必ず読み返す（語そのものが答えを教えてしまう）
- **固有名詞はデータと突合する**。ゴール文の取引先名・品番が実在し、表記が一致すること
- **ペルソナ設定を添える**（PC 習熟度・急ぎ具合・他システム経験）

**正解キーを先に採ります**（ペルソナの自己申告は信用しないため）。

- 「**UI に露出している実数**」から問いを作る。DB にしかない値では答えようがない
- 採取は **DB 復元の後・起動の直前**（復元でデータが変わると正解も変わる）
- 🚨 **正解を書いたファイルを、ペルソナの出力先ディレクトリに置かない**
  （作業ディレクトリのファイルを読まれて走行を破棄した実例あり）

### 3. 環境を整える

**`references/preparation.md` を読んで、チェックリストを消化してください。**
特に飛ばしてはいけないもの:

- **本番では走らせない**（ペルソナは削除も一括更新も遠慮なく実行する）
- **対象リポジトリの外から起動する** — `CLAUDE.md` はペルソナのコンテキストに自動ロードされ、
  実装規約を知った状態になる（封印の最大の穴）
- **DOM 自動保存の掃除役を回す** — スクショのたびに DOM が yml で保存され、応答にリンクまで付く
- **認証の通し方を決める** — ログイン必須なら、決めておかないと開始 URL で詰む
- **サーバーの同一性を確認する** — HTTP 200 は他人のサーバーでも返る

### 4. ペルソナを起動する

**単体なら Agent ツールで spawn します。** `subagent_type` に `ux-blind-tester` を指定し、
ゴール文をそのままプロンプトとして渡すだけです。親セッションの中で完結し、結果も直接返ります。

- **name は付けない**（付けると完了後も idle 通知が届き続ける）
- ペルソナは**あなたの MCP 接続を共有します**。スクリーンショットの落下先は
  playwright MCP の `--output-dir`（未指定なら起動時の作業ディレクトリの `.playwright-mcp/`）
  なので、**回収前に場所を確認**してください
- 所要は 1 体 6〜25 分。3 体以上なら**開始時にユーザーへ目安を伝える**

**複数体を同時に走らせるときだけ、CLI で独立プロセスとして起動します。**
Agent ツールの subagent は親セッションの MCP 接続を共有するため、
**2 体目以降が同じブラウザを叩いて操作が混線します**（タブを分けても解決しません）。
腕ごとに playwright の設定ファイルを分けてください。

```bash
claude --agent ux-blind-tester \
  --mcp-config <腕ごとの設定ファイル> --strict-mcp-config \
  -p "$(cat goal-a.txt)"
```

- **`--permission-mode auto` が要る場合があります**（非対話なので許可を聞けず、既定だと
  全ツールが拒否される。しかも失敗がペルソナの主観報告として返るので気づきにくい）
- **完了判定は出力ファイルが空でなくなること。停滞判定はスクリーンショットの更新時刻**
  （ログはまとめ書きされるので、止まって見えても動いていることがある）

> ⚠️ この配布物で実際に通したのは **CLI 経路のみ**です。Agent ツールでの spawn は
> 実運用の方式ですが、配布物としては未検証です。

### 5. 回収して答え合わせ

1. **ログを読む** — 手順ごとの「見たもの / 期待 / 操作 / 結果 / 迷い」
2. **「良かった点まとめ」も読む** — 維持すべき設計の収集は、つまずきの収集と同価値
3. **DB で裏取りする** — 達成できたかではなく、**登録内容が正しいか**まで見る
4. **封印を監査する** — 走行のトランスクリプトから、スクリーンショット以外を読んでいないか

   ```bash
   grep -o '"name":"Read","input":{"file_path":"[^"]*"' <transcript>.jsonl \
     | sed 's/.*file_path":"//' | grep -v 'jpeg' | sort | uniq -c
   # 出力が空なら封印 OK
   ```

   あわせて、画面に出ていない内部用語・モデル名をペルソナが使っていないか見る
   （使っていたら `CLAUDE.md` 経由で知識が入っている）

5. **偽陽性を潰す** — 「AI にしか起きない現象」をバグとして報告してきます。
   見つけたら `agents/ux-blind-tester.md` の行動ルールに追記してください
   （数値欄への流し込みで旧値が保存される・文字列の追記が全置換になる・
   macOS で `Ctrl+A` が効かない・自動で消える通知が撮影間隔とぶつかる）

### 6. レポートを出す

**摩擦ファーストで書きます。「ゴール達成 N/N」を先頭に置かないでください。**

```
## ヘッドライン（最重要のつまずき・経路ズレ・複数体が同じ場所で誤ったか）
## 定量（ペルソナ別の 手数 / 誤操作 / 詰まり / ギブアップ地点）
## 確定所見（つまずき箇所・ペルソナの生の声・改善案）
## 良かった点（維持すべき設計）
## プロトコル違反監査（破棄した走行とその理由。なければ「違反なし」）
## テスト環境ノイズ（除外したもの）
```

「ゴール達成 N/N」は参考行に降格し、**「※達成は直感性の証拠にしない（完了バイアス）」を
必ず併記**してください。

客観複雑度（AI に効かない負担の数値化）を測る場合と、修正前後の A/B で改善を証明する場合は
**`references/complexity-metrics.md`** に従います。A/B では**変えていいのは対象コードだけ**で、
ゴール文・ペルソナ設定・環境は凍結します。

---

## 前提

- playwright MCP が `--isolated --caps=vision` で使えること
- `ux-blind-tester` agent が `~/.claude/agents/` にあること
- ペルソナのモデルは Sonnet 級（弱いモデルは知覚が先に壊れて別の失敗をする）
